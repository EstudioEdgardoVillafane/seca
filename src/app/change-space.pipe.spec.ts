import { ChangeSpacePipe } from './change-space.pipe';

describe('ChangeSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
